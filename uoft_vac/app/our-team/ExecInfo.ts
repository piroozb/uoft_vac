// name, position, info, artwork, colour.
export type ExecDataTypes = [string, string, string, string?, string?];
export const execData: Record<string, ExecDataTypes> = {
    Sy: [
        "Sy",
        "President",
        "2nd year, Health & Disease/Cell Molecular Bio\nFun fact: I LOVE POKEMON AND SPACE",
    ],
};
